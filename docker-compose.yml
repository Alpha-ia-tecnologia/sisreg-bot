services:
  sisreg-bot:
    build: .
    ports:
      - "${PORT}:8080"
    env_file:
      - .env
    volumes:
      - bot-data:/app/data
    dns:
      - 8.8.8.8
      - 1.1.1.1
    networks:
      - easypanel
      - bridge
    restart: unless-stopped
volumes:
  bot-data:
networks:
  easypanel:
    external: true
  bridge:
    external: true
    name: bridge
